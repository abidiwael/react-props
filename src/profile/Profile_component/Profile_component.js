import React from 'react'

function Profile_component ({PersonData}) 
{
    return (
        <div>
            <h1>{fullName}</h1>
            <h4>{bio}</h4>
        </div>
    );
}

export default Profile_component
